# Agent Office Implementation Work Order

Implementation pending — this is the starting point for Codex implementation, not the final deliverable.

## Task
- Task ID: 363b258f-9a3e-8153-a329-c184b7f6a630
- Task name: Add Post-Merge Closeout v0
- Notion status at preview: In Codex
- Notion URL: https://www.notion.so/Add-Post-Merge-Closeout-v0-363b258f9a3e8153a329c184b7f6a630

## Branch and PR Starting Point
- Repository: SherifHaidar/chief-of-staff-agent-office
- Base branch: main
- Base commit: 47ff1915ee8a15a7d50f4c5ff66b13e3c5f1e8b3
- Branch: agent-office/impl-add-post-merge-closeout-v0-363b258f
- Draft PR title: [Draft] Implementation pending: Add Post-Merge Closeout v0
- Work order path: .agent-office/work-orders/363b258f-9a3e-8153-a329-c184b7f6a630.md

## Next Action
Codex must implement on this branch, run relevant tests, and return evidence before human merge or deploy approval.

Codex should continue on this branch, inspect the product repository directly, make the real implementation commits, run the relevant checks, and report evidence before human approval.

## Approved Codex Handoff Summary

### Problem Summary
The Agent Office can create work-order PRs and run review, but after Sherif merges a PR the associated Notion task still requires manual closeout. Implement a Post-Merge Closeout v0 action that verifies an already-merged PR, previews evidence and planned Notion updates, then commits the task closeout record idempotently after explicit operator confirmation.

### Product Intent
Complete the Agent Office lifecycle record after Sherif has already merged a PR, so merged work is moved out of active implementation queues with reliable PR/deployment evidence captured in Notion, without giving the Office authority to merge, approve, or deploy.

### Implementation Scope
- Add a narrow Post-Merge Closeout lifecycle desk/action to `SherifHaidar/chief-of-staff-agent-office`.
- Implement two explicit actions/modes: `Preview closeout` and `Commit closeout`.
- Add typed input/evidence/preview/commit result contracts for post-merge closeout.
- Add or reuse read-only GitHub PR merge evidence collection by `{ repo, prNumber }`.
- Add or reuse best-effort read-only deployment evidence lookup from the existing Office convention, if one exists.
- Add Notion closeout planning and commit logic for task properties plus a human-readable page block.
- Add deterministic idempotency detection for the Post-Merge Closeout block.
- Add diagnostics for verification, planned writes, commit writes, skipped fields, deployment lookup, and idempotency.
- Add mocked tests/fixtures based on the reference cases: PR #20 in `SherifHaidar/chief-of-staff-agent-office` and PR #6 in `SherifHaidar/personal-chief-of-staff`.
- Update Agent Office docs or operator help text only as needed to explain the new closeout action.

### Likely Affected Files or Modules
- `SherifHaidar/chief-of-staff-agent-office` — exact paths were not included in the provided repo context; Codex must inspect the repo before editing.
- Existing Review + Iteration Desk files in Agent Office — inspect and reuse conventions for operator actions, preview/commit workflow, GitHub evidence, Notion writeback, and diagnostics.
- Existing Agent Office API/action route layer — add a post-merge-closeout route/action only if that matches current conventions.
- Existing Agent Office GitHub service/client module — add read-only PR merge evidence lookup or reuse existing implementation.
- Existing Agent Office Notion service/client module — add task property update, page block append, and idempotency block detection logic.
- Existing Agent Office deployment/Vercel evidence module, if present — add/read best-effort production deployment lookup.
- Existing Agent Office schemas/types module — add typed contracts for post-merge closeout.
- Existing Agent Office diagnostics/logging module — add structured closeout diagnostics.
- Existing Agent Office tests/fixtures — add mocked PR #20 and PR #6 style scenarios.
- Agent Office README/operator docs — update only if needed to document the new action.

### Constraints / Do Not Change
- Do not mutate `SherifHaidar/personal-chief-of-staff`; the approved task is for the Agent Office lifecycle system, despite the wrapper target repo context pointing at the product app.
- Do not touch Chief of Staff product app flows: capture processing, memory, Tasks DB routing, voice transcription, Notion project save logic, Quick Task behavior, or `app/page.tsx`.
- Do not merge PRs, approve PRs, deploy production, trigger Vercel deployments, or infer Sherif’s final approval.
- Do not perform live Notion writes during development/testing unless Sherif explicitly authorizes the specific live test.
- Do not infer or mutate a Notion task without an explicit `notionTaskId` / task page ID from the operator.
- Keep v0 narrow: repo + PR number + Notion task ID in; operational closeout record out.
- Reuse existing Agent Office Review + Iteration Desk conventions for schemas, services, API/action shape, diagnostics, and operator UI/actions; do not create a parallel framework.
- GitHub and deployment integrations must be read-only.
- Notion writes are allowed only in explicit commit mode after preview and operator confirmation.
- Deployment evidence is optional/best-effort; absence must be visible but non-blocking for a confirmed merged PR.
- Idempotency is mandatory; duplicate closeout blocks are a blocker.
- If exact Notion task property names/status options cannot be discovered from the Agent Office repo/config, stop and request Sherif’s decision rather than guessing live write behavior.
- No broad UI redesign, no new review logic, no Codex dispatch changes, and no autonomous lifecycle progression beyond this closeout action.

### Implementation Steps
- Start by checking out `SherifHaidar/chief-of-staff-agent-office` from `main`. Do not work in `SherifHaidar/personal-chief-of-staff` for this task.
- Inspect the existing Review + Iteration Desk implementation and identify its conventions for operator input, preview/commit actions, schemas/types, GitHub evidence collection, Notion writeback, diagnostics, route/action structure, and tests.
- Locate Agent Office configuration for the Notion task database/page schema. Confirm canonical property names for status, PR URL/link, merge SHA, merged-at, repository, base branch, head branch, deployment evidence, and any production URL/status fields. If not discoverable, document the gap and ask Sherif before live write behavior.
- Define narrow typed contracts: `PostMergeCloseoutInput`, `PostMergeCloseoutEvidence`, `PostMergeCloseoutDeploymentEvidence`, `PostMergeCloseoutPreview`, `PostMergeCloseoutCommitResult`, and diagnostics types, following existing schema style.
- Implement or reuse a read-only GitHub lookup that returns PR URL, merged boolean, merge SHA, merged-at timestamp, base branch, head branch, and merged-by when available. Ensure there are no merge/approve methods in this flow.
- Implement hard-stop validation: preview and commit must fail clearly when the PR is not found, inaccessible, or not merged.
- Implement or reuse best-effort deployment evidence lookup. Prefer existing Office/Vercel/GitHub deployment evidence patterns. Return typed `missing` or `unavailable` states when credentials or evidence are absent.
- Build the closeout planner used by preview and commit. It should produce exact Notion property updates and the exact Post-Merge Closeout block from the same evidence object.
- Design the idempotency marker before writing. Recommended marker: a compact deterministic token such as `post-merge-closeout:{repo}#{prNumber}:{mergeSha}` included in the appended block in a low-noise way.
- Implement `Preview closeout`: gather GitHub/deployment evidence, compute planned Notion updates/block, run idempotency check if read-only block read is available, and return diagnostics. Do not mutate Notion.
- Implement `Commit closeout`: require explicit commit mode/action, re-run GitHub merged verification, recompute evidence/planned writes, check for existing marker, update supported Notion task properties, append the closeout block only if marker is not already present, and return written/skipped diagnostics.
- Ensure setting status to `Merged` is the only mechanism used to remove the task from active Implementation Ready queues unless the existing Office schema has a canonical additional lifecycle property.
- Add operator-facing UI/action wiring consistent with the existing desk/action pattern. Keep labels explicit: `Preview closeout` and `Commit closeout`.
- Add mocked tests for: merged PR preview with planned writes; merged PR commit writing properties/block; unmerged PR rejection; missing/unavailable deployment evidence; repeated closeout idempotency; and partial pre-existing closeout backfill if feasible.
- Run the Agent Office repo’s existing lint/build/test commands. If commands differ from the product app, use the Agent Office `package.json` scripts.
- Prepare a PR for Sherif review. Do not merge it and do not perform production deployment.

### Tests to Run
- Inspect Agent Office `package.json` and run its existing lint command, if present.
- Inspect Agent Office `package.json` and run its existing test command, if present.
- Inspect Agent Office `package.json` and run its existing build/typecheck command, if present.
- Run mocked test: preview closeout for merged PR #20 in `SherifHaidar/chief-of-staff-agent-office` returns evidence and planned Notion writes without mutation.
- Run mocked test: commit closeout for merged PR writes status/properties and appends one block.
- Run mocked test: PR #6 in `SherifHaidar/personal-chief-of-staff` can be used as external repo evidence while still writing only to the supplied Agent Office Notion task.
- Run mocked test: unmerged PR is rejected in preview and commit.
- Run mocked test: missing/unavailable deployment evidence is recorded but does not block closeout for a merged PR.
- Run mocked test: repeated commit with same repo/PR/merge SHA skips duplicate block append and reports idempotency.
- Manual dry-run: preview mode displays exact property updates, closeout block marker, and diagnostics clearly before any commit.
- Only with explicit Sherif approval: perform one live Notion preview/commit against a safe task and verify the task leaves active Implementation Ready queues by status change.

### Acceptance Checklist
- Implementation is made in `SherifHaidar/chief-of-staff-agent-office`, not in `SherifHaidar/personal-chief-of-staff`.
- Post-Merge Closeout accepts explicit `repo`, `prNumber`, and `notionTaskId` / Notion task page ID input.
- Preview mode verifies via GitHub that the PR exists and is already merged, then returns evidence and planned Notion writes without mutating Notion.
- Commit mode re-verifies the PR is merged before writing to Notion.
- Unmerged, closed-unmerged, missing, or inaccessible PRs are rejected in both preview and commit modes.
- Closeout evidence includes PR URL, repo, PR number, merge SHA, merged-at timestamp, base branch, head branch, and merged-by when available.
- Deployment evidence is best-effort and may be recorded as missing/unavailable without blocking closeout when GitHub confirms the PR is merged.
- Preview shows exact Notion task property updates and exact Post-Merge Closeout block content before commit.
- Commit updates the Notion task lifecycle/status to the canonical merged value, expected to be `Merged` unless existing Office schema says otherwise.
- Commit writes available PR/deployment fields only where supported by the existing Notion task schema; unsupported fields are reported, not silently invented.
- Commit appends exactly one Post-Merge Closeout block with a deterministic idempotency marker based on repo, PR number, and merge SHA.
- Repeated commit for the same repo/PR/merge SHA does not append duplicate closeout blocks and only backfills missing properties.
- Diagnostics show GitHub verification result, Notion task target, deployment lookup result, properties planned/written/skipped, idempotency result, and preview vs commit mode.
- No code path merges PRs, approves PRs, triggers deployments, or marks final approval on Sherif’s behalf.
- Mocked tests cover merged preview, merged commit, unmerged PR rejection, missing deployment evidence, and repeated closeout idempotency.
- Existing Agent Office build/lint/test commands pass.

## Draft PR Body
```markdown
## Implementation pending
Implementation pending — this is the starting point for Codex implementation, not the final deliverable.

## What Agent Office created
- Repository: SherifHaidar/chief-of-staff-agent-office
- Base: main @ 47ff1915ee8a15a7d50f4c5ff66b13e3c5f1e8b3
- Branch: agent-office/impl-add-post-merge-closeout-v0-363b258f
- Task: Add Post-Merge Closeout v0 (363b258f-9a3e-8153-a329-c184b7f6a630)
- Work order: `.agent-office/work-orders/363b258f-9a3e-8153-a329-c184b7f6a630.md`

## Next action
- Codex must implement on this branch, run relevant tests, and return evidence before human merge or deploy approval.
- Codex should check out this branch, inspect the product repo directly, edit the real product files, run tests, and push implementation commits to this PR.

## Approved handoff summary
Problem: The Agent Office can create work-order PRs and run review, but after Sherif merges a PR the associated Notion task still requires manual closeout. Implement a Post-Merge Closeout v0 action that verifies an already-merged PR, previews evidence and planned Notion updates, then commits the task closeout record idempotently after explicit operator confirmation.

Product intent: Complete the Agent Office lifecycle record after Sherif has already merged a PR, so merged work is moved out of active implementation queues with reliable PR/deployment evidence captured in Notion, without giving the Office authority to merge, approve, or deploy.

### Implementation scope
- Add a narrow Post-Merge Closeout lifecycle desk/action to `SherifHaidar/chief-of-staff-agent-office`.
- Implement two explicit actions/modes: `Preview closeout` and `Commit closeout`.
- Add typed input/evidence/preview/commit result contracts for post-merge closeout.
- Add or reuse read-only GitHub PR merge evidence collection by `{ repo, prNumber }`.
- Add or reuse best-effort read-only deployment evidence lookup from the existing Office convention, if one exists.
- Add Notion closeout planning and commit logic for task properties plus a human-readable page block.
- Add deterministic idempotency detection for the Post-Merge Closeout block.
- Add diagnostics for verification, planned writes, commit writes, skipped fields, deployment lookup, and idempotency.
- Add mocked tests/fixtures based on the reference cases: PR #20 in `SherifHaidar/chief-of-staff-agent-office` and PR #6 in `SherifHaidar/personal-chief-of-staff`.
- Update Agent Office docs or operator help text only as needed to explain the new closeout action.

### Likely affected files or modules
- `SherifHaidar/chief-of-staff-agent-office` — exact paths were not included in the provided repo context; Codex must inspect the repo before editing.
- Existing Review + Iteration Desk files in Agent Office — inspect and reuse conventions for operator actions, preview/commit workflow, GitHub evidence, Notion writeback, and diagnostics.
- Existing Agent Office API/action route layer — add a post-merge-closeout route/action only if that matches current conventions.
- Existing Agent Office GitHub service/client module — add read-only PR merge evidence lookup or reuse existing implementation.
- Existing Agent Office Notion service/client module — add task property update, page block append, and idempotency block detection logic.
- Existing Agent Office deployment/Vercel evidence module, if present — add/read best-effort production deployment lookup.
- Existing Agent Office schemas/types module — add typed contracts for post-merge closeout.
- Existing Agent Office diagnostics/logging module — add structured closeout diagnostics.
- Existing Agent Office tests/fixtures — add mocked PR #20 and PR #6 style scenarios.
- Agent Office README/operator docs — update only if needed to document the new action.

### Tests to run
- Inspect Agent Office `package.json` and run its existing lint command, if present.
- Inspect Agent Office `package.json` and run its existing test command, if present.
- Inspect Agent Office `package.json` and run its existing build/typecheck command, if present.
- Run mocked test: preview closeout for merged PR #20 in `SherifHaidar/chief-of-staff-agent-office` returns evidence and planned Notion writes without mutation.
- Run mocked test: commit closeout for merged PR writes status/properties and appends one block.
- Run mocked test: PR #6 in `SherifHaidar/personal-chief-of-staff` can be used as external repo evidence while still writing only to the supplied Agent Office Notion task.
- Run mocked test: unmerged PR is rejected in preview and commit.
- Run mocked test: missing/unavailable deployment evidence is recorded but does not block closeout for a merged PR.
- Run mocked test: repeated commit with same repo/PR/merge SHA skips duplicate block append and reports idempotency.
- Manual dry-run: preview mode displays exact property updates, closeout block marker, and diagnostics clearly before any commit.
- Only with explicit Sherif approval: perform one live Notion preview/commit against a safe task and verify the task leaves active Implementation Ready queues by status change.

## Approval boundary
- This draft PR is a starting point for implementation, not the final deliverable.
- Agent Office has not generated or committed product-code changes.
- This PR must not be merged or deployed until Codex implementation, review, tests, and final human approval are complete.
```

## Approval Boundary
- Agent Office created this work-order commit only.
- Product application files still need to be implemented by Codex on this branch.
- Do not merge or deploy until implementation, review, tests, and final human approval are complete.

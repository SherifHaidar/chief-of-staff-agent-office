# Agent Office Implementation Work Order

Implementation pending — this is the starting point for Codex implementation, not the final deliverable.

## Task
- Task ID: 364b258f-9a3e-8199-9d57-f6b8d01a099a
- Task name: Add Codex Dispatch v0
- Notion status at preview: In Codex
- Notion URL: https://www.notion.so/Add-Codex-Dispatch-v0-364b258f9a3e81999d57f6b8d01a099a

## Branch and PR Starting Point
- Repository: SherifHaidar/chief-of-staff-agent-office
- Base branch: main
- Base commit: d5b7d0226e7b970086a7a9059c785affa719e687
- Branch: agent-office/impl-add-codex-dispatch-v0-packet-workflow-364b258f
- Draft PR title: [Draft] Implementation pending: Add Codex Dispatch v0 packet workflow
- Work order path: .agent-office/work-orders/364b258f-9a3e-8199-9d57-f6b8d01a099a.md

## Next Action
Codex must implement on this branch, run relevant tests, and return evidence before human merge or deploy approval.

Codex should continue on this branch, inspect the product repository directly, make the real implementation commits, run the relevant checks, and report evidence before human approval.

## Approved Codex Handoff Summary

### Problem Summary
Add Codex Dispatch v0 to the Agent Office lifecycle so an operator can prepare and record a deterministic Codex-ready dispatch packet from an existing work-order PR/task, eliminating manual copy-paste of repo/branch/PR/work-order metadata while preserving confirmation, auditability, and review gates.

### Product Intent
Reduce Sherif’s manual bridge between Agent Office work-order PR creation and Codex implementation by producing an auditable packet that tells Codex exactly which repo, branch, PR, and work-order file to use, without auto-running Codex or weakening human/review controls.

### Implementation Scope
- Implement Codex Dispatch v0 as an Agent Office lifecycle capability for existing work-order PRs/tasks.
- Add or extend a small typed dispatch service adjacent to existing work-order PR lifecycle code after inspecting the Agent Office repo.
- Create a planner/validator that builds a typed dispatch input only from selected work-order metadata.
- Create a deterministic Markdown packet renderer for Codex instructions.
- Create a preview action/handler that returns validation status, packet Markdown, proposed write targets, proposed status/next action, and blocking errors without writes.
- Create a record action/handler that requires explicit confirmation and writes dispatch packet/status using existing Agent Office Notion/GitHub conventions.
- Add a disabled direct-dispatch adapter boundary only if useful for future extensibility; it must return unavailable/not configured in v0.
- Add focused tests around validation, rendering, preview/record separation, and safety boundaries.
- Update relevant Agent Office docs or inline help so operators understand the packet-only workflow and next action.

### Likely Affected Files or Modules
- Agent Office repo only: `SherifHaidar/chief-of-staff-agent-office` — exact paths unknown from the supplied Product Context Pack and must be inspected before implementation.
- Existing lifecycle/work-order PR creation modules in Agent Office — Codex must locate these.
- Existing Notion integration/update modules in Agent Office — Codex must locate and reuse these.
- Existing GitHub PR/comment/writeback modules in Agent Office — Codex must locate and reuse these if PR writeback is selected.
- Potential new Agent Office module such as `codex-dispatch`, `dispatch-planner`, `dispatch-recorder`, or adjacent service files, named to match repo conventions after inspection.
- Potential Agent Office tests adjacent to lifecycle/dispatch service tests.
- Potential Agent Office docs/readme/operator guide for the packet-only workflow.
- Do not affect `SherifHaidar/personal-chief-of-staff` files; product app repo context was inspected but is not the correct implementation surface for this task.

### Constraints / Do Not Change
- Repository target conflict exists in the supplied context: the Product Context Pack target is `SherifHaidar/personal-chief-of-staff`, but the approved Architect Brief explicitly says this task belongs in `SherifHaidar/chief-of-staff-agent-office` and must not be implemented in the Chief of Staff product app runtime. Codex must treat Agent Office as the implementation repo for this task.
- Do not edit Chief of Staff product app files such as `app/page.tsx`, `lib/notion.ts`, `app/api/*`, `lib/capture-schema.ts`, or `app/globals.css` for this task.
- Do not automatically trigger Codex after work-order PR creation.
- Do not execute Codex directly or call any presumed Codex API/tool in v0.
- Do not merge PRs.
- Do not deploy production.
- Do not bypass Review + Iteration Desk.
- Do not let Codex choose or infer a different repo/branch than the selected work-order specifies.
- Do not use broad personal GitHub tokens, new Notion scopes, or new credential classes unless Sherif explicitly approves.
- Do not add or assume new Notion database properties for dispatch status/next action/packet links unless existing Agent Office conventions support them or Sherif approves the schema change.
- Preview/planning code must be isolated from writer clients so no external writes can happen in preview mode.
- Fail closed on incomplete/inconsistent metadata; do not silently degrade to defaults such as `main`.
- Use one primary auditable packet artifact and, if updating both Notion and GitHub, make the secondary system point to the primary artifact to avoid duplicated sources of truth.
- Keep packet wording precise: `packet prepared/recorded`, not `Codex started/dispatched`, unless a future approved direct-dispatch capability exists.

### Implementation Steps
- Before editing, check out/inspect `SherifHaidar/chief-of-staff-agent-office` on its current `main` branch. Do not implement this task in `SherifHaidar/personal-chief-of-staff`.
- Locate the existing Architecture → Codex Handoff → work-order PR creation flow, task selection surface, Notion update path, GitHub PR/comment/writeback path, and any existing lifecycle status conventions.
- Identify the current work-order PR metadata model: target repo owner/name, base branch/base commit if available, implementation branch, PR number/url, work-order file path, Notion task id/url/title, lifecycle status, and scope summary/source brief.
- Choose the smallest existing operator surface for v0 after inspection, such as an existing dashboard action, command, API route, or lifecycle action. Do not wire dispatch as an automatic callback after work-order PR creation.
- Define typed dispatch inputs/results near the existing lifecycle modules: `CodexDispatchInput`, `CodexDispatchPreviewResult`, record confirmation input, validation error type, and dispatch status/next action values.
- Implement `DispatchPlanner` validation that normalizes selected work-order metadata and fails closed on missing target repo, branch, PR, work-order path, or Notion task. Add repo/branch consistency checks against the work-order PR metadata.
- Implement deterministic packet rendering in a pure function. The packet should prominently repeat target repo, implementation branch, PR number/url, work-order file path, Notion task, scope, safety boundaries, and required next gate.
- Implement `Prepare Codex Dispatch` preview flow as pure planner + renderer. It should return packet content and proposed write targets/status but must not receive or call Notion/GitHub writer clients.
- Implement `Confirm/Record Codex Dispatch` flow that accepts explicit confirmation and either the preview packet plus version/hash or regenerates from the same selected metadata. Record only after confirmation.
- For record writeback, reuse existing Agent Office Notion conventions. If dispatch status/next-action properties already exist, use them; otherwise prefer an existing comment/block/page-content convention or stop for Sherif schema approval.
- If GitHub PR writeback is consistent with existing Agent Office lifecycle patterns, record the packet as a PR comment or committed artifact. Pick one primary artifact; make Notion point to it or vice versa.
- Record next action in precise language, e.g. `Open Codex with the dispatch packet for repo X, branch Y, PR Z, work-order file W. After implementation, send to Review + Iteration Desk.`
- Optionally add a `DirectCodexDispatcher` interface only as a disabled future adapter returning `unavailable_not_configured`; do not expose it as a working v0 path.
- Add tests using mocks/stubs for Notion/GitHub writers proving preview does not write and record mode writes only after explicit confirmation.
- Validate against PR #21 if available in Agent Office history: confirm the generated packet contains the exact repo, branch, PR, work-order file, Notion task, safety boundaries, and review gate that Sherif previously had to copy manually.
- Run the Agent Office repo’s existing lint/typecheck/test/build commands. If command names differ, inspect `package.json` or project docs and run the closest equivalent.
- Prepare an implementation PR summary that clearly states packet-only dispatch, no direct Codex execution, and no production deployment.

### Tests to Run
- Inspect Agent Office `package.json` and run the repo’s existing test command, if present.
- Run the Agent Office lint command, if present.
- Run TypeScript/typecheck command, if present.
- Run build command, if present.
- Unit test: complete work-order metadata produces a stable deterministic dispatch packet.
- Unit test: missing repo, branch, PR, work-order file, or Notion task fails validation.
- Unit test: branch/repo mismatch between selected work-order metadata and PR metadata fails validation.
- Unit test: preview mode does not call mocked Notion or GitHub writers.
- Unit test: record mode refuses to write without explicit confirmation.
- Unit test: record mode writes expected status/next action using existing mocked Notion/GitHub conventions after confirmation.
- Unit test: direct Codex dispatcher returns unavailable/not configured and cannot trigger execution.
- Manual/reference test: use PR #21 metadata if available and verify the packet contains the repo, branch, PR, work-order file, Notion task, scope, safety boundaries, and Review + Iteration Desk next gate.

### Acceptance Checklist
- Codex Dispatch v0 is implemented in `SherifHaidar/chief-of-staff-agent-office`, not in `SherifHaidar/personal-chief-of-staff`.
- Operator can explicitly select or invoke an existing work-order PR/task and run a `Prepare Codex Dispatch`-style preview action.
- Preview mode is side-effect-free: no Notion writes, no GitHub comments, no repository commits, and no dispatch status mutation.
- Preview output includes a deterministic Codex-ready packet with: Notion task title/id/url, target repo, base branch/commit when known, implementation branch, work-order PR number/url, work-order file path, scope summary, safety boundaries, and Review + Iteration Desk as next gate.
- Validation fails closed when target repo, implementation branch, PR number/url, work-order file path, or Notion task metadata is missing or inconsistent.
- Validation does not infer repo/branch from environment defaults, current checkout, local git state, or free-form conversational text.
- Record mode requires explicit operator confirmation and writes only the already-previewed packet/status or a regenerated packet from the same validated metadata.
- Recorded status uses precise language such as `Codex Dispatch packet recorded`; it must not imply Codex has started.
- Packet tells Codex to work only in the specified repo/branch/PR/work-order file and not to merge, deploy, switch repos/branches, or bypass review.
- Direct Codex dispatch is not functional in v0; any adapter is disabled/unavailable by design.
- Existing Agent Office Notion/GitHub integrations are reused; no broad new personal tokens or scopes are introduced.
- Tests cover deterministic packet rendering, missing metadata failure, branch/repo mismatch failure, preview no-write behavior, record confirmation requirement, direct dispatch disabled behavior, and safety boundary inclusion.
- Reference pain point from PR #21 is validated by generating/confirming a packet containing the metadata Sherif previously had to copy manually, if the repo history exposes the needed PR/work-order data.

## Draft PR Body
```markdown
## Implementation pending
Implementation pending — this is the starting point for Codex implementation, not the final deliverable.

## What Agent Office created
- Repository: SherifHaidar/chief-of-staff-agent-office
- Base: main @ d5b7d0226e7b970086a7a9059c785affa719e687
- Branch: agent-office/impl-add-codex-dispatch-v0-packet-workflow-364b258f
- Task: Add Codex Dispatch v0 (364b258f-9a3e-8199-9d57-f6b8d01a099a)
- Work order: `.agent-office/work-orders/364b258f-9a3e-8199-9d57-f6b8d01a099a.md`

## Next action
- Codex must implement on this branch, run relevant tests, and return evidence before human merge or deploy approval.
- Codex should check out this branch, inspect the product repo directly, edit the real product files, run tests, and push implementation commits to this PR.

## Approved handoff summary
Problem: Add Codex Dispatch v0 to the Agent Office lifecycle so an operator can prepare and record a deterministic Codex-ready dispatch packet from an existing work-order PR/task, eliminating manual copy-paste of repo/branch/PR/work-order metadata while preserving confirmation, auditability, and review gates.

Product intent: Reduce Sherif’s manual bridge between Agent Office work-order PR creation and Codex implementation by producing an auditable packet that tells Codex exactly which repo, branch, PR, and work-order file to use, without auto-running Codex or weakening human/review controls.

### Implementation scope
- Implement Codex Dispatch v0 as an Agent Office lifecycle capability for existing work-order PRs/tasks.
- Add or extend a small typed dispatch service adjacent to existing work-order PR lifecycle code after inspecting the Agent Office repo.
- Create a planner/validator that builds a typed dispatch input only from selected work-order metadata.
- Create a deterministic Markdown packet renderer for Codex instructions.
- Create a preview action/handler that returns validation status, packet Markdown, proposed write targets, proposed status/next action, and blocking errors without writes.
- Create a record action/handler that requires explicit confirmation and writes dispatch packet/status using existing Agent Office Notion/GitHub conventions.
- Add a disabled direct-dispatch adapter boundary only if useful for future extensibility; it must return unavailable/not configured in v0.
- Add focused tests around validation, rendering, preview/record separation, and safety boundaries.
- Update relevant Agent Office docs or inline help so operators understand the packet-only workflow and next action.

### Likely affected files or modules
- Agent Office repo only: `SherifHaidar/chief-of-staff-agent-office` — exact paths unknown from the supplied Product Context Pack and must be inspected before implementation.
- Existing lifecycle/work-order PR creation modules in Agent Office — Codex must locate these.
- Existing Notion integration/update modules in Agent Office — Codex must locate and reuse these.
- Existing GitHub PR/comment/writeback modules in Agent Office — Codex must locate and reuse these if PR writeback is selected.
- Potential new Agent Office module such as `codex-dispatch`, `dispatch-planner`, `dispatch-recorder`, or adjacent service files, named to match repo conventions after inspection.
- Potential Agent Office tests adjacent to lifecycle/dispatch service tests.
- Potential Agent Office docs/readme/operator guide for the packet-only workflow.
- Do not affect `SherifHaidar/personal-chief-of-staff` files; product app repo context was inspected but is not the correct implementation surface for this task.

### Tests to run
- Inspect Agent Office `package.json` and run the repo’s existing test command, if present.
- Run the Agent Office lint command, if present.
- Run TypeScript/typecheck command, if present.
- Run build command, if present.
- Unit test: complete work-order metadata produces a stable deterministic dispatch packet.
- Unit test: missing repo, branch, PR, work-order file, or Notion task fails validation.
- Unit test: branch/repo mismatch between selected work-order metadata and PR metadata fails validation.
- Unit test: preview mode does not call mocked Notion or GitHub writers.
- Unit test: record mode refuses to write without explicit confirmation.
- Unit test: record mode writes expected status/next action using existing mocked Notion/GitHub conventions after confirmation.
- Unit test: direct Codex dispatcher returns unavailable/not configured and cannot trigger execution.
- Manual/reference test: use PR #21 metadata if available and verify the packet contains the repo, branch, PR, work-order file, Notion task, scope, safety boundaries, and Review + Iteration Desk next gate.

## Approval boundary
- This draft PR is a starting point for implementation, not the final deliverable.
- Agent Office has not generated or committed product-code changes.
- This PR must not be merged or deployed until Codex implementation, review, tests, and final human approval are complete.
```

## Approval Boundary
- Agent Office created this work-order commit only.
- Product application files still need to be implemented by Codex on this branch.
- Do not merge or deploy until implementation, review, tests, and final human approval are complete.
